# Copilot Instructions

## Рекомендации по проекту
- For chart UX, the user prefers trading-terminal behavior: in live mode zoom should anchor to the right edge, in manual mode zoom should behave like a market chart; preset buttons should be treated as zoom presets and considered active when current span is within ±10% of the preset.